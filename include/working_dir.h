#ifndef WORKING_DIR_H
#define WORKING_DIR_H

#define WDIR "../"
/**
 * @brief Enum representing the four cardinal directions.
 *
 * This enum is used to represent the four cardinal directions: left, right, up, and down.
 * It is typically used in games or simulations where directional movement is required.
 */
typedef enum {
    LEFT,   ///< Represents the left direction.
    RIGHT,  ///< Represents the right direction.
    UP,     ///< Represents the up direction.
    DOWN,   ///< Represents the down direction.
} Dir;
#endif
